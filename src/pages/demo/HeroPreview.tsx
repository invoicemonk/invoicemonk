import { HeroDashboardPreview } from '@/components/public/HeroDashboardPreview';

export default function HeroPreview() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-background via-background to-muted/30 flex items-center justify-center p-6 sm:p-10">
      <HeroDashboardPreview />
    </div>
  );
}
